#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Hello world!\n");
    printf("Next \nLine");
    printf("I am \" Developer\n");

    // Formate Specifier (%d = number, %s = string, %f = floating number (decimal), %c = single character)
    int favNum = 15;
    char grade = 'b';
    printf("%d",500);
    printf("My roll number is %d and favorite %s is %d and point is %f and grade is %c", favNum, "number",500, 20.5, grade);
    return 0;
}
